# Device Management — Response Models

> 3 models

## DeviceInfosModel

Source: `model/device_infos_model.dart`

```
ble_bind_type, ble_mac, connect, device_infos, product_code, sn,
wifi_bind_type
```

## DeviceListModel

Source: `model/device_list_model.dart`

```
auto_switch, bindSiteStatus, charging_pile_list, combineBox_List,
combiner_box_list, create_time, device_img, device_name, device_pn, device_sn,
forbidDeleted, grid_list, home_backup_system_list, isSelected, pps_list,
priority, running_time, site_id, site_img, site_info, site_name,
smart_plug_list, solar_list, solarbank_list, solarbank_pps_list, status,
tag_img
```

## DeviceModel

Source: `base:bean/model/device_model.dart`

```
access_group, device_id, group_id, integrator, station_id
```

